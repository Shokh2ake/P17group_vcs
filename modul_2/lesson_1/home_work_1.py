# class Mylist:
#     def __init__(self, massiv):
#         self.massiv = massiv
#     def append(self, items):
#         self.massiv = self.massiv + [items]
#     def pop(self, index:int = -1):
#         re_vel = self.massiv[index]
#         del self.massiv[index]
#         return re_vel
#     def sort(self, reverse = False):
#         self.massiv = sorted(self.massiv, reverse=reverse)
#
# a = Mylist([2, 4, 5, 1, 40, 3])
# # a.append(0)
# # a.pop(2)
# print(a.sort())
# # print(a.massiv)



class Mycar:
    def __init__(self, nomi, rangi, narxi, otkuchi, yili):
        self.nomi = nomi
        self.rangi = rangi
        self.narxi = narxi
        self.otkuchi = otkuchi
        self.yili = yili
    # def














